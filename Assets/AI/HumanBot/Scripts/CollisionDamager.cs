using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionDamager : MonoBehaviour
{
	/*
	public Head vrHead;
	public Head fpsHead;

	private int vrLayer;
	private int fpsLayer;

	private void Start()
	{
		vrLayer = LayerMask.NameToLayer("VRPlayer");
		fpsLayer = LayerMask.NameToLayer("FPSPlayer");
	}

	private void OnCollisionEnter(Collision collision)
	{
		if(collision.collider.gameObject.layer == vrLayer)
		{
			vrHead.Hit(collision.relativeVelocity.magnitude);		
		} else if (collision.collider.gameObject.layer == fpsLayer)
		{
			fpsHead.Hit(collision.relativeVelocity.magnitude);
		}
	}
	*/
}
