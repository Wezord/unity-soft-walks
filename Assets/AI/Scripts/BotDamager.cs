using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BotDamager : Damager
{
    /*
    public BotAgent agent;
    
    protected new void Start()
    {
		head = agent.p.target;
        base.Start();   
    }

    protected new void FixedUpdate()
    {
        if (agent.p.training) return;
        base.FixedUpdate();
    }
    */
}

